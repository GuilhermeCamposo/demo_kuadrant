apiVersion: grafana.integreatly.org/v1beta1
kind: Grafana
metadata:
  name: grafana
  namespace: '{{ grafana_project }}'
  labels:
     app: rhcl
spec:
  config:
    auth:
      disable_signout_menu: 'true'
    auth.anonymous:
      enabled: 'false'
    log:
      level: debug
      mode: console
    security:
      admin_password: test
      admin_user: test
  route:
    spec: {}

