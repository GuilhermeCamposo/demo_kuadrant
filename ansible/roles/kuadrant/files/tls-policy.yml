apiVersion: kuadrant.io/v1
kind: TLSPolicy
metadata:
  name: ingress-gateway-tls
spec:
  targetRef:
    name: ingress-gateway
    group: gateway.networking.k8s.io
    kind: Gateway
  issuerRef:
    group: cert-manager.io
    kind: ClusterIssuer
    name: kuadrant-ca-issuer